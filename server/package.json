{
  "name": "umbry-server",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Umbry settings + account sync backend (pure-JS: no native modules)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "@fastify/cors": "^9.0.1",
    "@fastify/rate-limit": "^9.1.0",
    "fastify": "^4.28.1",
    "hash-wasm": "^4.11.0",
    "jsonwebtoken": "^9.0.2"
  }
}
